import { ShowForLoginUsersDirective } from './show-for-login-users.directive';

describe('ShowForLoginUsersDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForLoginUsersDirective();
    expect(directive).toBeTruthy();
  });
});
