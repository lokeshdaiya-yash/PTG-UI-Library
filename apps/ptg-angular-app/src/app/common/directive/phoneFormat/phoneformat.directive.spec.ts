import { PhoneformatDirective } from './phoneformat.directive';

describe('PhoneformatDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneformatDirective();
    expect(directive).toBeTruthy();
  });
});
