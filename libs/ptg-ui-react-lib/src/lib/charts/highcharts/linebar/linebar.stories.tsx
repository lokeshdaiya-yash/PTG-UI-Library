import type { Meta } from '@storybook/react';
import { PtgUiLineBar } from './linebar';

const Story: Meta<typeof PtgUiLineBar> = {
  component: PtgUiLineBar,
  title: 'Other/PtgUiLineBar',
  tags: ['autodocs'],
};
export default Story;

export const Primary = {
  args: {},
};
