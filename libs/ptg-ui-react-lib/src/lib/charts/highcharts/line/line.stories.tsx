import type { Meta } from '@storybook/react';
import { PtgUiLine } from './line';

const Story: Meta<typeof PtgUiLine> = {
  component: PtgUiLine,
  title: 'Other/PtgUiLine',
  tags: ['autodocs'],
};
export default Story;

export const Primary = {
  args: {},
};
