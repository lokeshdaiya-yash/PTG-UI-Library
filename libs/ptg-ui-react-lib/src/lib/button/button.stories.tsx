import type { Meta } from '@storybook/react';
import { PtgUiButton } from './button';

const Story: Meta<typeof PtgUiButton> = {
  component: PtgUiButton,
  title: 'PtgUiButton',
};
export default Story;

export const Primary = {
  args: {},
};
