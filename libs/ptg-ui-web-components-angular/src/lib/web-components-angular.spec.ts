import { webComponentsAngular } from './ptg-ui-web-components-angular';

describe('webComponentsAngular', () => {
  it('should work', () => {
    expect(webComponentsAngular()).toEqual('web-components-angular');
  });
});
