import { Meta, Story, ArgsTable, Canvas , Preview} from '@storybook/addon-docs';

import * as HighLineChartComponents from './high-line-chart.stories.ts'

import HighLineChartComponent from './high-line-chart.component';

<Meta title="Component/HighLineChartComponent/HighLineChartComponentDocs" component={ HighLineChartComponent }    />

 # HighLineChart Component

 ---

This chart is created using the Highcharts library. Highcharts provides a wide variety of charts.   

A line graph is a graphical representation of information that changes over a period of time.

You can plot it by using several points linked by straight lines. It comprises two axes called the “x-axis” and the “y-axis“.


We can import the LineChart  using the following importation : 
```
    import { ChartModule } from ' @ptg-ui/angular'

```    



 ## Quick Start
 
 we can create a highLineChart using the following tag: 

```
<ptg-ui-high-line-chart
  [data]="data"
  chartId="line-chart"
></ptg-ui-high-line-chart>

```

Following control is supported by Checkbox: 

chartId : 

The chartId attribute specifies a unique id for an chart . The value of the id attribute must be unique.

The id attribute is used to point to a specific style declaration in a style sheet. It is also used by other languages and manipulate the element with the specific id.


<Canvas><Story id="component-highlinechartcomponent--chart-id" /></Canvas>